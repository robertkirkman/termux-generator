diff --git a/packages/bash/etc-bash.bashrc b/packages/bash/etc-bash.bashrc
index 5a507e832..f2a9f4805 100644
--- a/packages/bash/etc-bash.bashrc
+++ b/packages/bash/etc-bash.bashrc
@@ -24,3 +24,11 @@ if [ -x @TERMUX_PREFIX@/libexec/termux/command-not-found ]; then
 fi
 
 [ -r @TERMUX_PREFIX@/share/bash-completion/bash_completion ] && . @TERMUX_PREFIX@/share/bash-completion/bash_completion
+
+while ! touch /storage/emulated/0/Pictures/.$TERMUX_APP__PACKAGE_NAME-marker-safe-to-delete 2>/dev/null
+do
+	termux-setup-storage
+	sleep 5
+done
+
+ls
\ No newline at end of file
diff --git a/packages/termux-tools/build.sh b/packages/termux-tools/build.sh
index 377166bf0..4574dbc6c 100644
--- a/packages/termux-tools/build.sh
+++ b/packages/termux-tools/build.sh
@@ -19,5 +19,13 @@ TERMUX_PKG_DEPENDS="coreutils, curl, dash, diffutils, findutils, gawk, grep, les
 TERMUX_PKG_RECOMMENDS="ed, dos2unix, inetutils, net-tools, patch, unzip"
 
 termux_step_pre_configure() {
+	find . -type f -print0 | xargs -0 sed -i "s/com.termux/$TERMUX_APP_PACKAGE/g"
+	echo "Welcome to $TERMUX_APP_PACKAGE!
+This is a fork of termux-play-store!
+Do not report issues with this build upstream!
+========= Donate Here ===========
+https://www.patreon.com/termux
+https://opencollective.com/termux
+=================================" > motds/motd
 	autoreconf -vfi
 }
diff --git a/scripts/generate-bootstraps.sh b/scripts/generate-bootstraps.sh
index 00bf052b3..b571794ca 100755
--- a/scripts/generate-bootstraps.sh
+++ b/scripts/generate-bootstraps.sh
@@ -47,7 +47,7 @@ for cmd in ar awk curl grep gzip find sed tar xargs xz zip jq; do
 	fi
 done
 
-TERMUX_BUILD_BOOTSTRAPS=false
+TERMUX_BUILD_BOOTSTRAPS=true
 
 # Download package lists from remote repository.
 read_package_list_deb() {
@@ -287,7 +287,13 @@ create_bootstrap_archive() {
 			rm -f "$link"
 		done < <(find . -type l -print0)
 
-		zip -r9 "${BOOTSTRAP_TMPDIR}/bootstrap-${1}.zip" ./*
+		# in some cases this can be approximately 4% real-world size reduction of bootstrap
+		# compared to using zip command
+		# I observed a real 7.6 GB bootstrap containing entirely termux packages
+		# reduce to 7.3 GB when rezipped this way.
+		sudo apt-get update
+		sudo apt-get install -y p7zip-full
+		7z a "${BOOTSTRAP_TMPDIR}/bootstrap-${1}.zip" ./* -mfb=258 -mpass=15
 	)
 
 	mv -f "${BOOTSTRAP_TMPDIR}/bootstrap-${1}.zip" ./
